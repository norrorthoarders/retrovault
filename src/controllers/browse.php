<?php
/*
 * browse_index() lived here, with templates/browse/index.php.
 *
 * It was a landing page offering ways into the catalogue - by platform, by category, by
 * decade - which is a menu about the collection rather than a view of it. Collection now
 * opens the overview, which answers "what have I got" directly, and the ways in are the
 * filters on the browser itself where they are useful while looking at results rather
 * than before you start.
 *
 * The file is kept so nothing that requires it fails; there is nothing to put in it.
 */
